# custom_keybindings.zsh
# Unbind any conflicting keybindings
bindkey -r '^ '
bindkey -r '^[k'
bindkey -r '^[j'

# Set custom keybindings
bindkey '^ ' autosuggest-accept
bindkey '^[k' history-substring-search-up
bindkey '^[j' history-substring-search-down
